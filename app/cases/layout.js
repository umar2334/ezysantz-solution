export const metadata = {
  title: "Cases",
  description: "Explore selected projects by Ezysantz Solution — real work delivered for real clients including web apps, mobile apps, and WordPress platforms.",
  alternates: { canonical: "/cases" },
};

export default function CasesLayout({ children }) {
  return children;
}
